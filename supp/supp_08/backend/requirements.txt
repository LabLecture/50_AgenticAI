# supp_08 백엔드 — day11~15 와 동일 스택(MLAPI). yeardream(py3.11) 에 이미 설치돼 있음.
fastapi==0.136.3
uvicorn[standard]==0.49.0
openai==2.41.0
python-dotenv==1.2.2
# uvicorn[standard] 에 websockets 포함 (WebSocket 실습용)
